{
  "name": "mangox-coach-backend-helpers",
  "private": true,
  "type": "module",
    "scripts": {
        "check": "tsc --noEmit",
        "eval:fixtures": "node -e \"JSON.parse(require('fs').readFileSync('scripts/coach-eval-fixtures.json','utf8')); console.log('coach-eval-fixtures.json OK')\""
    },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "jsonrepair": "^3.12.0",
    "zod": "^3.24.0"
  }
}
